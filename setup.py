from setuptools import find_packages, setup

setup(
    name="pyb",
    version="0.0.0",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
    python_requires=">=3.7",
)
